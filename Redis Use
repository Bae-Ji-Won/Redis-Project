- Redis에 접속하는 법
1. Redis 파일안에 bin폴더안에 접속
docker exec -it my-redis /bin/sh

2. Redis 접속
redis-cli

- Redis 간단 문법
1. 저장된 모든 키 출력
keys *

2. 키 전부 삭제
flushall

3. 해당 키 개수 구하기
ZCOUNT [키 이름] 0 1000        // 0 ~ 1000까지 사이에 포함되는 숫자의 개수 출력